[package]
name = "test-indracore-adder"
version = "0.2.0"
authors = ["Selendra <info@selendra.org>"]
description = "Test indracore which adds to a number as its state transition"
edition = "2021"
build = "build.rs"

[dependencies]
tiny-keccak = { version = "2.0.2", features = ["keccak"] }
dlmalloc = { version = "0.2.3", features = [ "global" ] }
parity-scale-codec = { version = "3.1.5", default-features = false, features = ["derive"] }

sp-std = { git = "https://github.com/paritytech/substrate", default-features = false , branch = "polkadot-v0.9.26" }
# We need to make sure the global allocator is disabled until we have support of full substrate externalities
sp-io = { git = "https://github.com/paritytech/substrate", default-features = false, features = [ "disable_allocator" ] , branch = "polkadot-v0.9.26" }

indracore = { package = "selendra-indracore", path = "../../../../indracore/core", default-features = false, features = [ "wasm-api" ] }

[build-dependencies]
substrate-wasm-builder = { git = "https://github.com/paritytech/substrate", branch = "polkadot-v0.9.26" }

[features]
default = [ "std" ]
std = [
	"indracore/std",
	"sp-std/std",
]
